# Local-only snapshot of the pre-migration project.
# It is intentionally not committed; use Git history to recover the original tree.
/legacy/

# Bun / JavaScript dependencies and build output
/node_modules/
/dist/
/coverage/
*.tsbuildinfo

# Local environment overrides (keep shared .env.example files tracked)
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
bun-debug.log*

# OS / editors
.DS_Store
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
